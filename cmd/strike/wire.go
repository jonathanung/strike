// Composition root for strike: assembles engine, host services, session store,
// and the TUI. Implementation lives in assemble_tools.go (providers/tools/MCP)
// and session_lifecycle.go (run/resume/exec session loops).
package main
